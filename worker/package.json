{
  "name": "gov-data-pipeline-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest",
    "db:migrate": "wrangler d1 migrations apply gov-data-pipeline-db --local",
    "db:migrate:prod": "wrangler d1 migrations apply gov-data-pipeline-db --remote"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5",
    "wrangler": "^4.66.0"
  }
}
